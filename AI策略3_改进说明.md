# 赤锋牌局：AI Strategy 3.1 / Mobile & Public Behavior Inference

## 本轮目标

1. 延长「碰 / 吃 / 杠 / 胡」电影式特效，让动作有足够停留时间，不再一闪而过。
2. 重做玩家 AI 分析建议和电脑对手的舍牌/鸣牌逻辑，修复“看起来很聪明但牌效很怪”的情况。
3. 建立严格的公开信息边界：玩家与 AI 对练时，电脑 AI 不能读取玩家暗手、其他电脑暗手、真实牌墙顺序或下一张补牌。
4. 保持 AI 辅助默认关闭。
5. 修复手机端吃/碰/杠响应条遮挡，明示来源玩家、目标牌和吃牌组合。
6. 手机与 PC 共用同一套 7 主题素材库，修正已发现的牌面绘制错误。

## 从复盘中定位到的旧 AI 问题

旧版本不是单纯“权重不够好”，而是决策管线有结构性偏差：

- 深度分析只给预筛选的少数候选真实 MC 预算，其他候选用平均/回退值补齐，导致前置启发式一旦排错，深度层会继续放大错误，而不是纠错。
- 字牌结构分对孤立字牌过于宽容，出现手牌中明显孤立的「发 / 白 / 南 / 东」仍被保留，而拆掉有连续潜力的数牌结构。
- 吃、碰使用固定奖励，缺少“鸣牌后必须再打一张什么”的完整后续搜索，所以很多局面会机械地出现「吃 98 / 过 58」。
- 电脑对手主要依赖局部形状启发式，基础向听、有效牌、公开危险度没有统一到与分析器相同的核心模型。
- 深度分析快照曾保留对手 `hand` 字段。即使当时主要评分函数没有显式使用这些值，这依然是不可接受的全图信息通道。

## AI Strategy 3.1

### 1. 基础牌效成为硬主轴

每个可打候选都先计算：

- 当前向听数；
- 精确有效牌种类及剩余枚数；
- 手牌局部连接、对子、刻子、搭子；
- 公开信息危险度；
- 牌型潜力；
- 两巡改良预期。

不同难度不再通过“故意犯基础牌效错误”区分。入门 AI 只会在同向听、接近等价候选中加入少量随机性；标准 AI 使用精确一阶牌效和公开风险；高级 AI 增加受限两巡搜索。

### 2. 深度分析不再制造候选偏置

所有舍牌候选得到相同规格的深度处理：

- 每个候选都计算一阶有效牌；
- 每个候选都执行受限两巡搜索；
- 每个候选获得相同大小、确定性 seed 的 MC 预算；
- MC 只作为低权重 tie-break，不允许盖过向听/有效牌主轴。

这样能避免“先被启发式排到前 3 → 才有 MC → 更容易继续排前”的自增强偏差。

### 3. 修复孤立字牌与拆搭倾向

- 单张孤立字牌现在有明显负分；
- 字牌对子、刻子仍保留真实价值；
- 两面/嵌张/边张、相邻牌、跨一张连接分别计价；
- 不再因为“字牌存在”就给传统麻将牌型潜力加分。

以本次复盘初始局面重建测试，旧分析把「四条」排第一、「发」排第八；新引擎把「发」恢复为第一候选，符合该局面“先清孤立字牌、保留条子连续结构”的基本牌效逻辑。

### 4. 吃 / 碰 / 杠改成“动作 + 后续舍牌”联合评估

不再给「吃」或「碰」固定高分。现在会：

1. 模拟鸣牌；
2. 对鸣牌后的剩余手牌搜索最佳舍牌；
3. 重算向听和有效牌；
4. 比较不开口的 pass 基线；
5. 加入门前灵活性 / 开口代价；
6. 如果鸣牌导致向听恶化或有效牌大幅塌缩，施加强惩罚；
7. 只有确实改善速度或结构时才积极鸣牌。

分析说明也会直接写出，例如：“吃 一条·二条·三条 后最佳续打 X；1向听，进张 N，已计开口代价”，而不是只显示一个没有上下文的「吃 98」。

### 5. 攻守切换使用公开信息

风险模型只允许使用：

- 各家已经打出的牌；
- 已公开副露；
- 四川规则下已经公开的定缺；
- 对局阶段 / 牌墙剩余张数；
- AI 自己的手牌。

接近听牌时防守权重较高；远离听牌时不让防守评分喧宾夺主。公开弃牌和副露越多，后期危险度权重越高。

### 6. 新增对手公开行为画像

AI 现在不再只统计“对手有几组副露”，还会使用：

- 对手吃、碰、杠的具体牌和花色；
- 弃牌的先后顺序；
- 最近 6 张弃牌是否显示花色转向；
- 碰牌后对第四张的补杠可能；
- 副露附近牌张与相同花色的相对危险度。

这些只是公开行为的概率倾向，不会反推或读取任何暗手真值。运行时可用 `window.__inferOpponentPublicIntent(pid, tile)` 查看某家的公开行为推断。

## 手机交互与牌面一致性

- 竖屏响应条放到手牌下方的独立区域，不再覆盖手牌或牌河。
- 横屏响应时将手牌暂时弱化，以完整响应条取代同一操作带，选择后立即恢复。
- 提示条会写明“响应谁的弃牌”、“目标是哪张”；按钮上会显示“碰 六条”、“杠 六条”或可吃组合。
- 自摸/自杠窗口会显示可暗杠、补杠的具体牌。
- 手机大厅新增与 PC 同源的 7 套牌面主题选择，两端共用同一个本地设置。
- 已核验并修正 `radical-max` 主题中“九万被绘成六万”的错牌；装饰主题额外显示规则牌名角标，避免花纹导致误认。

## 严格禁止 AI 开全图

新增 `aiBuildDecisionView(pid)`，电脑玩家获得的决策视图被结构化隔离：

- `ownHand`：只包含该 AI 自己的暗手；
- `players[]`：只有 `id / discards / melds / missing / handCount / done`；
- 不存在任何对手 `hand` 字段；
- 只有 `wallCount`，不存在真实 `wall` 数组和顺序。

玩家 AI 分析的深度快照也做了同样处理：

- 玩家自己的手牌可用；
- 其他三家 `hand=[]`，只保留 `hiddenCount`；
- 牌墙只保存等长 `-1` 占位，不保存任何牌值；
- 对手 `lastDraw` 也被清空。

明杠 / 暗杠 / 补杠的预期收益只根据公开牌池和剩余张数估算，绝不会读取“下一张补牌是什么”。

新增运行时审计接口：

```js
window.__auditAIInformationBoundary(1)
```

以及综合诊断：

```js
window.__runAIStrategyDiagnostics()
```

额外做了隐藏状态不变性测试：固定某个 AI 的自己手牌和全部公开信息，随后彻底改写另外三家的暗手以及真实牌墙内容/顺序；高级 AI 的选择和候选排序保持完全一致。这比仅检查“代码里似乎没读 hand”更强，因为它从行为层验证隐藏真值无法改变结果。

## 特效时间

| 动作 | 正常动态 | reduced-motion / Via 兼容动态 |
|---|---:|---:|
| 碰 | 1900 ms | 1482 ms |
| 吃 | 2000 ms | 1560 ms |
| 杠 | 2350 ms | 1833 ms |
| 胡 | 3200 ms | 2496 ms |

Via / Android WebView 即使报告 `prefers-reduced-motion: reduce`，现在也只降低粒子和快速位移，不会把核心动作动画压缩到 0.01ms。

## 已保留的产品行为

- AI 辅助默认状态仍为 `off`。
- 电脑 AI 和玩家的“AI 分析建议”共享同一套基础牌效思想，但电脑 AI 只看自己的暗手；玩家分析器只看玩家自己的暗手。
- 默认“宫廷象牙”仍保持原有清晰牌面；其他装饰主题加入规则牌名校验标识。
- 现有多分辨率拼贴 UI 继续保留。

## 验证重点

- 复盘初始局面回归：新 AI 第一候选由异常的「四条」修正为「发」。
- 复盘多个中盘节点重新建模后，推荐更偏向保持同向听且清理孤立牌，不再频繁为了表面进张数破坏已经形成的结构。
- 隐藏状态不变性测试：通过。
- 信息边界字段审计：通过。
- 特效 normal / reduced-motion 两套环境：四种动作全部通过。
- 1672×941、1366×900、1112×948、768×1024、844×390、390×844 的稳定布局回归已检查。
- 390×844 与 844×390 响应条/手牌碰撞检查：不相交。
- 手机主题数量与 PC 一致：7 套；错牌回退映射：通过。
- 公开行为模型测试：对手碰六条后，模型正确将条子识别为偏好花色；信息边界审计仍通过。
