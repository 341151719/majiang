# AI 策略 4.0：期望效用核心与 Benchmark

## 本次实现范围

本次按《算法优化.txt》的 P0/P1 顺序落地可独立验证的弃牌核心，不声称已经完成全文中的粒子信念、ISMCTS 或完整算分器：

- 规则配置统一管理牌种、每种牌张数、吃牌、多响与风险参数；
- 只由自己的手牌和全桌公开牌构造决策视图，不读取对手暗牌或牌墙顺序；
- 剩余牌统一按 `每种牌 4 张 - 已知公开张数` 计算；
- 风险从单一百分比改为 `放铳概率 × 条件损失`，并保留尾部损失；
- 对所有可能的一巡摸牌按剩余张数加权，并枚举摸牌后的合法弃牌；
- 同向听但改善牌形的路径进入未来价值，不再只奖励立刻降向听；
- 先做 Pareto 支配筛选，再按攻守期望效用排序，得分归一到 0–100；
- 有界缓存保证页面交互不会因精确的一巡枚举而卡顿。

尚未完成的部分是鸣牌/和牌的统一 `Q(action)-Q(pass)`、粒子信念、ISMCTS、离线训练与规则精确计分。这些应拆分为后续 PR，避免把未经验证的跨规则判断混入当前可工作的弃牌策略。

## 当前验证结果

运行：

```bash
node tests/ai-strategy-v4.test.js
node tests/ai-performance-cache.test.js
python tests/ui-visual-audit.py
```

固定公开状态的 Node 基准在本开发环境中观测到冷启动约 47 ms、缓存后 p95 约 0.7 ms；这些数字是回归基线，不是跨设备性能承诺。自动测试阈值分别放宽到 1000 ms，真实 Chromium 页面另设缓存后最大 200 ms 的守门条件。

测试还覆盖：隐藏牌与牌墙内容改变时根决策完全不变、公开弃牌改变时决策更新、公开副露计数、风险具有正的条件损失、同向听改善、Pareto 支配淘汰，以及候选值为有限数。

## 开源深度学习麻将 AI 调研

| 项目 | 规则与可用性 | 本项目中的定位 |
| --- | --- | --- |
| [IJCAI Mahjong 2026](https://github.com/SuuTTT/IJCAI-mahjong) | 国标麻将；公开了训练/评测代码、[模型](https://huggingface.co/Dannibal/ijcai-mahjong-ckpts-2026)与官方复局说明，模型仓库标注 MIT。源码仓库未见明确许可证，复用前需再次确认。 | 与传统四人玩法最接近。优先复用其同牌墙、换座位的 duplicate evaluation 方法；完成动作与计分适配前不能直接比较胜率。 |
| [Mortal](https://github.com/Equim-chan/Mortal) | 日本立直麻将，AGPL-3.0，深度强化学习代码完整；作者[明确说明](https://gist.github.com/Equim-chan/cf3f01735d5d98f1e7be02e94b288c56)强模型权重不公开。 | 可比较网络结构、训练管线和 mjai 接口，不能当作开箱即用的同规则对手。 |
| [Kanachan](https://github.com/Cryolite/kanachan) | 日本立直麻将的 Transformer / 模仿学习 / 离线强化学习框架；项目说明不附爬虫、训练数据和训练模型。 | 适合训练方案参考，不适合作为现成胜率基线。 |
| [Suphx](https://arxiv.org/abs/2003.13590) | 论文公开，介绍全局奖励预测、oracle guiding 和运行时策略适应；没有可直接接入本项目的开源实现。 | 仅作研究上限和方法参考，不纳入可复现成绩。 |
| [Mahjax](https://arxiv.org/abs/2605.20577) | 高吞吐日本立直麻将 JAX 环境，不是训练好的对手。 | 后续若构建学习型策略，可参考其批量模拟与可复现吞吐测试。 |

结论：目前找到的深度学习项目没有一个可以在“不改规则”的前提下直接成为本项目四川麻将的公平对手。最可信的近期路线是：传统四人玩法先适配 IJCAI 国标麻将评测接口；四川玩法则必须基于本项目规则训练或接入同规则代理。把立直麻将模型的名次或胜率直接抄为四川麻将 benchmark 会产生错误结论。

## 建议的可复现评测协议

1. 固定规则版本、随机种子和模型提交哈希，先通过合法动作、隐藏信息与确定性测试。
2. 对每副固定牌墙进行四次换座位对局；比较版本时双方使用完全相同的牌墙集合。
3. 分别报告平均得分/名次、和牌率、放铳率、非法动作数、每步 p50/p95/p99 耗时；不得只报单一胜率。
4. 决策级数据集记录公开状态、合法动作、各动作期望效用和最终回报，用 bootstrap 置信区间报告 regret 差异。
5. 传统与四川模式分开出报告。只有规则、动作语义和计分都一致时，才允许与外部模型做结果级比较。

当前 PR 完成的是第 1 步的核心守门测试和本地性能基线；外部模型适配、成规模 duplicate 对局以及统计置信区间仍是后续工作，尚无外部胜率结论。
